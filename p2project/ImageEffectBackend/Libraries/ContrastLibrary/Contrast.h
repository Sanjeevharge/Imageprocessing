#ifndef CONTRAST_H
#define CONTRAST_H
#include "../Pixel.h"
#include <vector>
using namespace std;
//the header file for constrast.cpp
void applyContrast(vector< vector<Pixel> > &imageVector,float &amount);

#endif
